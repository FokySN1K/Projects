Задача: Расставить N ферзей на доску N на N, чтобы они не били друг друга
Решим задачу с помощью рекурсии:
1. Будем подавать в функцию массив N на N, массив N(пусть будет называться arr), отвечающй за "знание" занятости строки и само N.
2. Сверяемся занят ли весь arr
2.1. Если да, то выводим массив с ферзями
2.2. Если нет, то в for отправляем те же данные в функцию(рекурсия)
........
def dfs(massiv, arr, N):
	if empty(massiv):
		print(massiv)
	else:
		massiv1 = massiv(переписываем все значения)
		arr1 = arr(также)
		for i in range(N):
			stay(massiv1, arr1, N, line, column) // устанавливаем ферзя в свободную ячейку(ориентируемся по arr и 1(бито))
			if not arr1 == arr(по занятым полям): // то есть ничего не изменилось
				dfs(massiv1, arr1, N)
				massiv1 = massiv
				arr1 = arr
		delete(massiv1)
		delete(arr)
P.S. Должна быть проверка на то, что ферзя действительно можно вставить
P.S.S. Когда устанавливаем ферзя можно сразу посмотреть на первую незанятую строку
P.S.S.S. Можно уменьшить кол-во вставок, если понять, что с "половины" решения становятся "перевернутыми"